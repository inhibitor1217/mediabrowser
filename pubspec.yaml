name: mediabrowser
description: This is a simple plugin for listing video files on external storage.
version: 0.1.0
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: io.inhibitor.mediabrowser
        pluginClass: MediaBrowserPlugin
